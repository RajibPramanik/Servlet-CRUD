# Servlet-CRUD

Create, Read, Update, and Delete, or CRUD, are the fundamental data management operations used in all applications. We can connect to a database and carry out these tasks via a web interface by using Java Servlets with JDBC.
